name: Dockerhub push pipeline

on:
  workflow_dispatch:
  # push:
  #   branches: ["main"]

jobs:
  dockerpush:
    runs-on: [self-hosted, Linux]

    steps:
      - name: Checkout code
        uses: actions/checkout@v6

      - name: Dockerhub push
        uses: karthipv-07/Git-demo-batch02/.github/actions/docker-build-push@main
        with:
          username: ${{ vars.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # - name: Login to Docker
      #   uses: docker/login-action@v4
      #   with:
      #     username: ${{ vars.DOCKERHUB_USERNAME }}
      #     password: ${{ secrets.DOCKERHUB_PASSWORD }}

      # - name: Build Docker image
      #   run: docker build -f Dockerfile-distroless -t dockerfile-distroless .

      # - name: Set Docker tag
      #   run: echo "SHORT_SHA=${GITHUB_SHA::7}" >> $GITHUB_ENV

      # - name: Tag Docker image
      #   run: docker tag dockerfile-distroless ${{ vars.DOCKERHUB_USERNAME }}/nodeapp2:${{ env.SHORT_SHA }}

      # - name: Push image
      #   run: docker push ${{ vars.DOCKERHUB_USERNAME }}/nodeapp2:${{ env.SHORT_SHA }}
